import { Point } from "../types";

export class RandomHelpers {
  static circleRandom(center: Point, radius: number) {}
  static pointRandom(min: Point, max: Point) {}
  static linearRandom(min: number, max: number) {}
}
